# Test generated by RoostGPT for test curriculum-simulator-backend-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/api/courses/all"
          expect:
            - statusCode: 200
            - hasProperty: "courses"

  - name: "API request with non-existing endpoint"
    flow:
      - get:
          url: "/api/courses/non-existing-endpoint"
          expect:
            - statusCode: 404

  - name: "API request with large data"
    flow:
      - loop:
          - get:
              url: "/api/courses/all"
          count: 10000
        expect:
          - statusCode: 200
          - hasProperty: "courses"

  - name: "API request with unauthorized access"
    flow:
      - get:
          url: "/api/courses/all"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
