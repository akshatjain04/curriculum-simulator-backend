# Test generated by RoostGPT for test curriculum-simulator-backend-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/"
          expect:
            - statusCode: 200
            - body: "home"
          # The API should return a status code of 200 and the body should contain "home"

  - name: "API request with not found endpoint"
    flow:
      - get:
          url: "/notfound"
          expect:
            - statusCode: 404
          # The API should return a status code of 404 as the endpoint does not exist

  - name: "API request with edge case"
    flow:
      - get:
          url: "/?param=special%20characters%20and%20large%20data"
          expect:
            - statusCode: 200
            - body: "home"
          # The API should handle special characters in the query parameters and return a status code of 200
