# Test generated by RoostGPT for test curriculum-simulator-backend-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/courses/section1"
          expect:
            - statusCode: 200
            - hasProperty: "courses"
            - hasProperty: "section"
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/courses/invalidSection"
          expect:
            - statusCode: 400
  - name: "API request with edge case"
    flow:
      - get:
          url: "/courses/section1?specialChars=*&^%$#@!"
          expect:
            - statusCode: 200
            - hasProperty: "courses"
            - hasProperty: "section"
      - get:
          url: "/courses/section1?largeData=LoremIpsumIsSimplyDummyTextOfThePrintingAndTypesettingIndustry."
          expect:
            - statusCode: 200
            - hasProperty: "courses"
            - hasProperty: "section"
