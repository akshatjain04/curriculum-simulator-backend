# Test generated by RoostGPT for test curriculum-simulator-backend-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/program"
          json:
            program: [{"id": 1, "name": "Course 1"}, {"id": 2, "name": "Course 2"}]
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "pae"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/program"
          json:
            program: [{"id": "", "name": "Course 1"}, {"id": 2, "name": "Course 2"}]
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/program"
          json:
            program: [{"id": 1, "name": "Course 1"}, {"id": 2, "name": "Course 2"}, {"id": 3, "name": "Course with special characters !@#$%^&*()"}]
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "pae"
